<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="RadioProductAttribute" owl="1">
        <t t-set="ref_rate"
           t-value="env.services.pos.pos_session.tax_today or (env.services.pos.config.show_currency_rate != 0 ? (1/env.services.pos.config.show_currency_rate) : 1)"/>
        <div class="configurator_radio">
            <div class="table">
                <t t-foreach="values" t-as="value" t-key="value.id">
                    <div class="attribute-name-cell">
                        <input type="radio" t-model="state.selected_value" t-att-name="attribute.id"
                               t-attf-id="{{ attribute.id }}_{{ value.id }}" t-att-value="value.id"/>
                        <label t-attf-for="{{ attribute.id }}_{{ value.id }}">
                            <t t-esc="value.name"/>
                        </label>
                    </div>

                    <div t-if="value.price_extra" class="price-extra-cell">
                        <label t-attf-for="{{ attribute.id }}_{{ value.id }}">
                            <span class="price_extra">
                                + <t t-esc="env.utils.formatCurrency(value.price_extra)"/>
                            </span>
                            /
                            <span class="price_extra" style="background-color:#f27474;">
                                + <t t-esc="env.services.pos.format_currency_ref(value.price_extra * ref_rate)"/>
                            </span>
                        </label>
                    </div>

                    <div t-if="value.id == state.selected_value and value.is_custom" class="custom-value-cell">
                        <label t-attf-for="{{ attribute.id }}_{{ value.id }}">
                            <input class="custom_value" type="text" t-model="state.custom_value"/>
                        </label>
                    </div>
                </t>
            </div>
        </div>
    </t>

    <t t-name="SelectProductAttribute" owl="1">
        <t t-set="ref_rate"
           t-value="env.services.pos.pos_session.tax_today or (env.services.pos.config.show_currency_rate != 0 ? (1/env.services.pos.config.show_currency_rate) : 1)"/>
        <div>
            <t t-set="is_custom" t-value="false"/>

            <select class="configurator_select" t-model="state.selected_value">
                <option t-foreach="values" t-as="value" t-key="value.id" t-att-value="value.id">
                    <t t-set="is_custom" t-value="is_custom or (value.is_custom and value.id == state.selected_value)"/>
                    <t t-esc="value.name"/>
                    <t t-if="value.price_extra">
                        + <t t-esc="env.utils.formatCurrency(value.price_extra)"/>
                        /
                        <t t-esc="env.services.pos.format_currency_ref(value.price_extra * ref_rate)"/>
                    </t>
                </option>
            </select>

            <input class="custom_value" t-if="is_custom" type="text" t-model="state.custom_value"/>
        </div>
    </t>

</templates>