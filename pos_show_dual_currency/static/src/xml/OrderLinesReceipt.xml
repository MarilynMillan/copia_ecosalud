/** @odoo-module */

// 1. Importaciones de Odoo v17
import { OrderlineReceipt } from "@point_of_sale/app/screens/receipt_screen/orderline_receipt/orderline_receipt";
import { patch } from "@web/core/utils/patch";
import { xml } from "@odoo/owl";

// 2. Parcheamos la plantilla (el .XML) de OrderlineReceipt
patch(OrderlineReceipt, {
    template: xml`
        <t t-patch="OrderlineReceipt" t-patch-mode="append">
            <xpath expr="//span[hasclass('price-tag')]" position="before">
                <span class="pos-receipt-right-align" style="float: right; margin-right: 10px;">
                    <t t-esc="env.pos.format_currency_no_symbol(props.line.get_price_with_tax_sec())"/>
                </span>
            </xpath>
        </t>
    `,
});